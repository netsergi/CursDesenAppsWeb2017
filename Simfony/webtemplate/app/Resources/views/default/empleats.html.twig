{% extends 'base.html.twig' %}

{% block main %}

<div class="w3-main" style="margin-left:250px">

  <div class="w3-row w3-padding-64">
  {% block showemp %}
      {% for empleat in resultat %}
      <div class="w3-card-4 card_emp">
      <header class="w3-container">
          <img class="icono_emp" src="{{ asset ('img/empleat/User'~random(2)~'.png')}}" />
          <h1 class="w3-text-teal"><a href="{{ path('empleat', {'id' : empleat.EMPL_ID}) }}">{{empleat.EMPL_FORNAME}} {{empleat.EMPL_SURNAME}}</a> <span class="w3-badge w3-blue emp_id"> {{empleat.EMPL_NUMBER}}</span> <span class="emp_id">Num. Empleat</span></h1>
          <hr>
           {{ render(controller("AppBundle:Default:sidebar", {'id' : empleat.EMPL_ID } )) }}
        </header>
      </div>        
      <br>
     {% endfor %}
    </div>
   </div>  
   {% endblock %}
  <!-- Pagination -->
  <div class="w3-center w3-padding-32">
    <div class="w3-bar">
      <a class="w3-button w3-black" href="#">1</a>
      <a class="w3-button w3-hover-black" href="#">2</a>
      <a class="w3-button w3-hover-black" href="#">3</a>
      <a class="w3-button w3-hover-black" href="#">4</a>
      <a class="w3-button w3-hover-black" href="#">5</a>
      <a class="w3-button w3-hover-black" href="#">»</a>
    </div>
  </div>

{% endblock %}